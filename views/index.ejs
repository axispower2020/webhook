<!DOCTYPE html>

<html>
    <head>
        <title>Web Hook</title>
        <link rel="stylesheet" href="https://normalize-css.googlecode.com/svn/trunk/normalize.min.css" />
        <link rel="stylesheet" href="stylesheets/style.css" />
    </head>
    
    <body ng-app="myApp" ng-controller="myCtrl">
        <h2>Welcome to the WebHook System (for issue title and description)!</h2>
        <div>
            <h3>Current Hook(s)</h3>
            <table>
                <% for(i in register){ %>
                    <tr>
                        <% for(j in register[i]){ %>
                            <td>
                                <%= register[i][j] %>
                            </td>
                        <% } %>
                    </tr>
                <% } %>
            </table>

            <form action="/register" method="post">
                <div>
                    <label>User-name:</label>
                    <input type="text" name="user" required />
                    <br/>
                </div>
                <div>
                    <label>Repo:</label>
                    <input type="text" name="repo" required />
                </div>
                <div>
                    <label>Type(s):</label>
                    <input type="checkbox" name="type" value="title" /> Title   
                    <input type="checkbox" name="type" value="desc" /> Description
                </div>
                <div>
                    <input type="submit" value="Add New Hook" />
                </div>
            </form>
        </div>
        
        <hr>
        
        <div>
            <h3>Current Target(s)</h3>
            <table>
                <% for(i in target){ %>
                    <tr>
                        <% for(j in target[i]){ %>
                            <td>
                                <%= target[i][j] %>
                            </td>
                        <% } %>
                    </tr>
                <% } %>
            </table>

            <form action="/target" method="post">
                <div>
                    <label>Link-to-push: http(s)://</label>
                    <input type="text" name="link" required />
                    <br/>
                </div>
                <div>
                    <input type="submit" value="Add New Target" />
                </div>
            </form>
        </div>
    
    
    
        
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
        <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.3.13/angular.min.js"></script>
    
        <script src="javascripts/app.js"></script>
    </body>
</html>